server:
  port: 8071
spring:
  application:
    name: config-server
  profiles:
    active:
      - git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aviNelson/config.git
          password:
          username:
